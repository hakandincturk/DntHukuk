@model IEnumerable<DosyaDurumu>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dosyaDurumuTuru)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.dosyaDurumuTuru)
            </td>
            <td>
                <a asp-controller="" asp-action="Edit" asp-route-id="@item.dosyaDurumuId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.dosyaDurumuId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.dosyaDurumuId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
